import { Layout } from '@/components/Layout'

export default function DashboardPage() {
  return (
    <Layout title="看板">
      <p className="text-neutral-500">统计看板将在后续版本提供（上游密钥用量、代理密钥用量、请求日志、成功率与延迟）。</p>
    </Layout>
  )
}
