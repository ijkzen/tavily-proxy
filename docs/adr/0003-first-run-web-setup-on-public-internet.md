# 公网部署的单用户系统采用 Web 首访引导注册

系统是单用户的（无注册、无多租户），但服务暴露在公网（tavily.ijkzen.cn）。初始账号通过 Web 首访引导创建：首次访问时若无用户则展示创建表单，账号创建后该页面永久关闭。

**Considered Options**：通过环境变量/CLI 预置账号（更安全，无窗口期）——因部署便利性和个人项目规模被拒绝。

**接受的权衡**：从部署完成到首次访问之间存在「谁先访问谁就能成为用户」的窗口期。缓解措施：部署后应立即完成首访注册；如未来需要，可加「启动日志打印一次性 setup token」作为第二道闸（当前未实现）。
