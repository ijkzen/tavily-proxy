# tavily-proxy

一个暴露在公网的单用户代理服务：代理 Tavily 的全部 MCP 能力，在多个 Tavily 官方密钥之间做额度感知的负载均衡。

## Language

**上游密钥（Upstream Key）**:
Tavily 官方签发的 API key（`tvly-...`），是密钥池的成员，代理用它向 Tavily 发起真实请求。
_Avoid_: API key（裸用，歧义）、Tavily key、原始 key

**代理密钥（Proxy Key）**:
本应用签发给调用方的 key（`tp-...`），MCP 客户端用它连接本服务；与上游密钥是完全不同的概念。
_Avoid_: API key（裸用，歧义）、用户 key、访问令牌

**密钥池（Key Pool）**:
全部上游密钥的集合，负载均衡在其上进行；每个成员有独立的额度与健康状态。
_Avoid_: key 列表、密钥组

**用户（User）**:
本系统唯一的人类账号，通过 Web 登录体系访问管理界面。系统是单用户的——没有注册、没有多租户。
_Avoid_: 管理员、租户、客户

**额度（Quota）**:
上游密钥所属 Tavily 账号在计费周期内的 credits 总量与剩余量。Tavily 按账号粒度计量——同账号的多个 key 共享同一额度；负载均衡按"有效剩余额度最多"选 key。
_Avoid_: 配额、余额（Tavily 按 credits 计，非金额）

## 密钥状态

上游密钥的健康状态，由负载均衡的状态机维护：

**冷却（Cooling）**:
上游密钥因 429 限流被短暂移出选路（默认 60 秒），到期自动恢复。
_Avoid_: 限流中、暂停

**耗尽（Exhausted）**:
上游密钥因 432/433 被判额度用尽，直到计费周期重置日才恢复选路。
_Avoid_: 超限、欠费

**禁用（Disabled）**:
上游密钥因 401 失效或被用户手动停用，需人工干预才恢复。
_Avoid_: 删除、拉黑
