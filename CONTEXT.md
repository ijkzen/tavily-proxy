# tavily-proxy

一个暴露在公网的单用户代理服务：代理 Tavily / Exa 的搜索与提取能力，在多个上游密钥之间做**分组随机 + 组内轮询**的负载均衡（ADR-0004）。

## Language

**上游密钥（Upstream Key）**:
Tavily 官方 key（`tvly-...`）或 Exa 官方 key（`exa-...`），是密钥池的成员，代理用它向上游发起真实请求。每个密钥带 `kind`（tavily / exa）标识提供商。
_Avoid_: API key（裸用，歧义）、Tavily key、原始 key

**提供商（Provider）**:
Tavily 或 Exa——一类上游服务的抽象（基址、鉴权方式、端点、参数风格、用量单位）。负载均衡先按提供商分组，再组内轮询。
_Avoid_: 上游（歧义，指外部服务）、供应商

**代理密钥（Proxy Key）**:
本应用签发给调用方的 key（`tp-...`），MCP 客户端用它连接本服务；与上游密钥是完全不同的概念。
_Avoid_: API key（裸用，歧义）、用户 key、访问令牌

**密钥池（Key Pool）**:
全部上游密钥的集合，负载均衡在其上进行；每个成员有独立的提供商、额度与健康状态。
_Avoid_: key 列表、密钥组

**用户（User）**:
本系统唯一的人类账号，通过 Web 登录体系访问管理界面。系统是单用户的——没有注册、没有多租户。
_Avoid_: 管理员、租户、客户

**额度（Quota）**:
上游密钥所属账号的用量与剩余量。Tavily 按账号粒度计量 credits（`GET /usage` 轮询为主数据源）；Exa 无公开余额接口，按「每月 10 美元」本地记账（`costDollars` 累减，每月 1 号重置）。额度用于**展示与排名**，不参与选路。
_Avoid_: 配额、余额（仅 Exa 场景可用「余额」描述美元）

## 密钥状态

上游密钥的健康状态，由负载均衡的状态机维护：

**冷却（Cooling）**:
上游密钥因 429 限流被短暂移出选路（默认 60 秒），到期自动恢复。
_Avoid_: 限流中、暂停

**耗尽（Exhausted）**:
上游密钥被判额度用尽（tavily 432/433，exa 402），直到计费周期重置日才恢复选路。
_Avoid_: 超限、欠费

**禁用（Disabled）**:
上游密钥因 401 失效或被用户手动停用，需人工干预才恢复。
_Avoid_: 删除、拉黑
